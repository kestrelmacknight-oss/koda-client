name: koda
description: Koda — privacy-first community platform for gamers and creators.
publish_to: 'none'
version: 0.34.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dio: ^5.4.3
  url_launcher: ^6.2.6
  livekit_client: ^2.8.1
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  file_picker: ^8.1.2
  phoenix_socket: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

